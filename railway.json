{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "/bin/sh -c 'pip install -r requirements.txt && python bot.py & python middleware_listener.py & while true; do python metrics_bot.py; sleep 300; done & wait'"
  }
}